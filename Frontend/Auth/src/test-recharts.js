// Simple test to verify recharts import
import {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
} from "recharts";

console.log("Recharts imported successfully!");
console.log("BarChart:", BarChart);
console.log("ResponsiveContainer:", ResponsiveContainer);

export {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
};
